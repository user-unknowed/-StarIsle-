name: SLSA Build Provenance - AI Engine

on:
  push:
    tags:
      - 'v*'
    branches:
      - main
  release:
    types: [created]
  workflow_dispatch:

permissions:
  contents: read
  packages: write
  id-token: write
  attestations: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}-ai-engine

jobs:
  build:
    name: Build AI Engine Container Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: './后台/ai-engine/requirements.txt'

      - name: Install dependencies and verify
        working-directory: ./后台/ai-engine
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Upload source artifact
        uses: actions/upload-artifact@v4
        with:
          name: starisle-ai-engine-src
          path: |
            后台/ai-engine/app/
            后台/ai-engine/requirements.txt
            后台/ai-engine/dockerfile

      - name: Generate source provenance attestation
        uses: actions/attest-build-provenance@v1
        with:
          subject-path: |
            后台/ai-engine/app/
            后台/ai-engine/requirements.txt
            后台/ai-engine/dockerfile

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        id: push
        uses: docker/build-push-action@v5
        with:
          context: ./后台/ai-engine
          file: ./后台/ai-engine/dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
          labels: |
            org.opencontainers.image.source=${{ github.repositoryUrl }}
            org.opencontainers.image.revision=${{ github.sha }}

      - name: Generate container image provenance
        uses: actions/attest-build-provenance@v1
        with:
          subject-name: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true
